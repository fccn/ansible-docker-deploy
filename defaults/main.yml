---
docker_deploy_base_folder: "/opt/docker-deploy"

# Optional parameters
# docker_deploy_compose_template: "docker-compose.yml.j2"
# docker_deploy_stack_template: docker-stack.yml

# To use docker-compose up / down using shell instead of docker-compose ansible role
# docker_deploy_shell: true

docker_deploy_folders: "{{ docker_deploy_files + docker_deploy_templates + docker_deploy_git_repositories }}"
docker_deploy_files: []
docker_deploy_templates: []
docker_deploy_git_repositories: []

docker_deploy_stack_pip_requirements:
  - jsondiff
  - pyyaml

docker_deploy_pip_packages:
  - package: docker-py
    package_fallback: docker-py==1.10.6
  - package: docker-compose
    package_fallback: docker-compose==1.8.0
  # selectors depends on the user_agent package without a specific release number. 
  # Newer user_agent version don't support python2.
  - package: user_agent 
    package_fallback: user_agent==0.1.9
  - package: selectors
    package_fallback: selectors==0.0.14

docker_deploy_packages: 
  - "python-pip"
  - "python-setuptools"

# Can be used to forcelly add the '--force-recreate' to the docker-compose up
#docker_deploy_shell_start_default_additional_parameters: 

#docker_deploy_shell_start_default: docker-compose pull && docker-compose up -d
