# Python requirements for development and testing
ansible>=4.0
ansible-lint>=6.0.0
yamllint>=1.26.0
molecule>=5.0.0
molecule-plugins[docker]>=23.0.0
docker>=6.0.0
jmespath>=1.0.0

# Note: Also install the community.docker collection:
# ansible-galaxy collection install community.docker
